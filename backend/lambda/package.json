{
  "name": "petadex-search-lambdas",
  "version": "1.0.0",
  "description": "AWS Lambda functions for PETadex sequence search",
  "type": "module",
  "scripts": {
    "build:submit-search": "cd submit-search && zip -r ../submit-search.zip .",
    "build:get-results": "cd get-results && zip -r ../get-results.zip .",
    "build:list-searches": "cd list-searches && zip -r ../list-searches.zip .",
    "build:all": "npm run build:submit-search && npm run build:get-results && npm run build:list-searches"
  },
  "dependencies": {
    "@aws-sdk/client-lambda": "^3.500.0",
    "@aws-sdk/client-s3": "^3.500.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
